{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://signaltrace.example/schema/cluster/v1",
  "title": "Synthetic Cluster",
  "description": "Schema defining a cluster of related synthetic messages or entities.",
  "type": "object",
  "additionalProperties": false,
  "required": ["cluster_id", "members"],
  "properties": {
    "cluster_id": {
      "type": "string",
      "description": "Identifier for the cluster. Must be unique and synthetic."
    },
    "members": {
      "type": "array",
      "description": "List of member identifiers belonging to this cluster.",
      "items": {
        "type": "string"
      }
    },
    "scores": {
      "type": "object",
      "description": "Optional map of scoring metrics for the cluster.",
      "additionalProperties": {
        "type": "number"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Optional metadata about the cluster.",
      "additionalProperties": true
    }
  }
}